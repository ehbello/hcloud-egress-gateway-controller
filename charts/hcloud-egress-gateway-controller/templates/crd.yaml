{{- /* Generated from api/v1alpha1 by "make manifests"; do not edit. */ -}}
{{- if .Values.installCRD }}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: hetzneregressgateways.egress.maarlab.dev
spec:
  group: egress.maarlab.dev
  names:
    kind: HetznerEgressGateway
    listKind: HetznerEgressGatewayList
    plural: hetzneregressgateways
    shortNames:
    - heg
    singular: hetzneregressgateway
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.readyGateways
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          HetznerEgressGateway pins the egress source IP(s) of selected pods to stable
          Hetzner floating IPs, spread across the nodes where the controller's agents run.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HetznerEgressGatewaySpec defines a stable-source-IP egress
              path for a set of pods.
            properties:
              backend:
                default: cilium
                description: Backend programs the egress redirect. Defaults to cilium.
                enum:
                - cilium
                - host-routing
                type: string
              destinationCIDRs:
                description: |-
                  DestinationCIDRs restricts which destinations are routed through the gateway.
                  Empty => 0.0.0.0/0 (any destination), with the cluster-internal ranges added
                  to ExcludedCIDRs automatically so in-cluster traffic is never redirected.
                items:
                  type: string
                type: array
              egressInterface:
                default: egr0
                description: |-
                  EgressInterface is the name of the dummy interface the agent creates on each
                  gateway node to hold that node's floating IP; the egress backend SNATs to this
                  interface's address, so N gateway nodes each SNAT to their own floating IP and
                  traffic spreads across them.
                type: string
              excludedCIDRs:
                description: |-
                  ExcludedCIDRs are destinations NOT routed through the gateway (extra to the
                  cluster-internal ranges the controller always excludes).
                items:
                  type: string
                type: array
              floatingIPs:
                description: |-
                  FloatingIPs is a BYO list of existing Hetzner floating IP addresses to adopt
                  (already created + whitelisted at the provider). Mutually exclusive with Managed.
                items:
                  type: string
                type: array
              managed:
                description: |-
                  Managed lets the controller create/own the floating IPs. Mutually exclusive
                  with FloatingIPs.
                properties:
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels are extra Hetzner labels stamped on the created floating IPs (the
                      controller always adds its own managed-by/gateway/index labels for adoption).
                    type: object
                  reclaimPolicy:
                    default: Retain
                    description: ReclaimPolicy on CR deletion. Defaults to Retain
                      (protects the whitelisted addresses).
                    enum:
                    - Retain
                    - Delete
                    type: string
                  regions:
                    description: |-
                      Regions is the set of Hetzner locations across which floating IPs are
                      provisioned. Spread them across the locations where your workloads run so each
                      region has a fixed egress IP and cross-region egress hops are avoided. NOTE:
                      Cilium's OSS egress gateway is not topology-aware — a single gateway spanning
                      regions distributes endpoints to gateway nodes by hash, not by locality; for
                      guaranteed same-region egress use one CR per region with a region-scoped
                      podSelector (and region-affine workloads).
                    items:
                      description: |-
                        ManagedRegion asks for a number of floating IPs in one Hetzner location. A floating
                        IP can only be assigned to a server in its home location, so the controller pins
                        this region's agents to nodes in that location (topology.kubernetes.io/region).
                      properties:
                        count:
                          description: |-
                            Count is the number of floating IPs to provision in this location (= gateway
                            nodes traffic spreads across within the region). Keep it small and stable:
                            every address must be whitelisted at the upstream provider.
                          minimum: 1
                          type: integer
                        location:
                          description: Location is the Hetzner location, e.g. "nbg1",
                            "fsn1", "hel1".
                          minLength: 1
                          type: string
                      required:
                      - count
                      - location
                      type: object
                    minItems: 1
                    type: array
                  type:
                    default: ipv4
                    description: Type of floating IP. Defaults to ipv4.
                    enum:
                    - ipv4
                    - ipv6
                    type: string
                required:
                - regions
                type: object
              podSelector:
                description: |-
                  PodSelector selects the pods whose egress must use the fixed source IPs.
                  Services opt in by carrying these labels — no node pinning.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
            required:
            - podSelector
            type: object
          status:
            description: HetznerEgressGatewayStatus is the observed state.
            properties:
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              floatingIPs:
                description: |-
                  FloatingIPs are the actual addresses (BYO + managed) and where they are assigned.
                  Whitelist these at the upstream provider.
                items:
                  description: FloatingIPStatus reports a floating IP's live state
                    — the address to whitelist.
                  properties:
                    address:
                      type: string
                    assignedNode:
                      type: string
                    hcloudID:
                      format: int64
                      type: integer
                  required:
                  - address
                  type: object
                type: array
              readyGateways:
                description: ReadyGateways is the count of floating IPs currently
                  assigned to a healthy node.
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
{{- end }}
